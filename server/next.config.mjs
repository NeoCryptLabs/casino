/** @type {import('next').NextConfig} */
export default {
  // le jeu (index.html, src/, assets/) vit à la racine du dépôt et est servi
  // par le serveur personnalisé, pas par Next : rien à configurer ici
  reactStrictMode: true,
};
